%%  cascade simple::_::layout { title => 'New Blogs',
%%      
%%  }

%%  override panel -> {
%%      for $blogs -> $blog {
%%          include 'simple/_/_blog_sidecard.tx' { blog => $blog, is_pending_link => 1 };
%%      }
%%  }

